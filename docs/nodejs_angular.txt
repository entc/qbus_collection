// get the version of the nodejs installer
npm -v

// get the version of nodejs
ng --version

// install a specific version of angular
sudo npm -g install @angular/cli@1.5.*

// install all packets needed by the project
npm install

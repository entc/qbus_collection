INCLUDE_DIRECTORIES( ".." "../core")

SET (PVD_TCP_SOURCES
  qbus_tcp.c
)

SET (PVD_TCP_HEADERS
  qbus_tcp.h
)

ADD_LIBRARY (qbus_pvd_tcp STATIC ${PVD_TCP_SOURCES} ${PVD_TCP_HEADERS})
TARGET_LINK_LIBRARIES (qbus_pvd_tcp cape)
#SET_TARGET_PROPERTIES (qbus_pvd_tcp PROPERTIES LIBRARY_OUTPUT_DIRECTORY $ENV{QBUS_PVD_BINDIR})

#INSTALL (TARGETS qbus_pvd_tcp DESTINATION mods/qbus)

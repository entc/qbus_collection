INCLUDE_DIRECTORIES( ".." "../../core")

SET (PVD_TCP_SOURCES
  qbus_tcp.c
)

SET (PVD_TCP_HEADERS
  qbus_tcp.h
)

ADD_LIBRARY (qbus_pvd2_tcp SHARED ${PVD_TCP_SOURCES} ${PVD_TCP_HEADERS})
TARGET_LINK_LIBRARIES (qbus_pvd2_tcp cape qbus_frame)
SET_TARGET_PROPERTIES (qbus_pvd2_tcp PROPERTIES LIBRARY_OUTPUT_DIRECTORY ${PVD_QBUS2_DIR})

INSTALL (TARGETS qbus_pvd2_tcp DESTINATION mods/qbus2)
